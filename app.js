const grid=document.querySelector('#grid'),search=document.querySelector('#search'),filter=document.querySelector('#filter');let owned=JSON.parse(localStorage.getItem('pokemonBinder')||'[]');let pokemon=[];
async function load(){document.querySelector('#progress').textContent='Lade Pokédex…';try{const r=await fetch('https://pokeapi.co/api/v2/pokemon?limit=151');const d=await r.json();pokemon=await Promise.all(d.results.map(async(p,i)=>{const s=await fetch(`https://pokeapi.co/api/v2/pokemon-species/${i+1}`).then(r=>r.json());const n=s.names.find(x=>x.language.name==='de');return {id:i+1,name:n?n.name:p.name}}));render()}catch(e){document.querySelector('#progress').textContent='Fehler beim Laden'}}
function render(){const q=(search.value||'').toLowerCase(),f=filter.value;grid.innerHTML='';pokemon.forEach((p)=>{const has=owned.includes(p.id);if(f==='owned'&&!has||f==='missing'&&has)return;if(q&&!p.name.toLowerCase().includes(q)&&!String(p.id).includes(q))return;const el=document.createElement('article');el.className='card '+(has?'owned':'');el.innerHTML=`${has?'<span class="check">✓</span>':''}<img loading="lazy" src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/home/${p.id}.png"><div class="num">#${String(p.id).padStart(3,'0')}</div><div class="name">${p.name}</div><div class="status">${has?'Habe ich':'Fehlt mir'}</div>`;el.addEventListener('click',()=>{owned=has?owned.filter(id=>id!==p.id):[...owned,p.id];localStorage.setItem('pokemonBinder',JSON.stringify(owned));render()});grid.appendChild(el)});document.querySelector('#progress').textContent=`${owned.length} / 151 Pokémon gesammelt`;document.querySelector('#bar').style.width=`${owned.length/151*100}%`}
search.addEventListener('input',render);filter.addEventListener('change',render);load();